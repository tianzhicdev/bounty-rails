# Intentionally-entropic PUBLIC files (same convention as C's ethkey-lite c17).
# index.html = the rails board: its watchlist/scam sections are curated tables
# of PUBLIC GitHub issue URLs, and long issue-path URLs trip the high-entropy
# heuristic (26 [LOW] findings, all 'com/...NNN' URL tails — zero credential
# shapes). The file is machine-generated by tools/bounty-scan/render_site.py,
# so per-line allow-comments would be wiped on every render; whole-path exclude
# is the right primitive here. Everything else (guide.html, robots, sitemap,
# future files) stays strictly scanned.
index.html
